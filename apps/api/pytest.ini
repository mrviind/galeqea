[pytest]
testpaths = tests
# Domain models are named TestCase/TestStep/TestStat; without this pytest tries
# to collect them as test classes and warns on every run.
python_classes =
python_files = test_*.py
python_functions = test_*
asyncio_mode = auto
